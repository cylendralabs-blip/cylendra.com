# OKX Demo Trading - تحديث مهم

## كيف يعمل OKX Demo

### ✅ الطريقة الصحيحة:

1. **نفس API Keys**: استخدم نفس مفاتيح API من حسابك الحقيقي
2. **اختر المنصة الصحيحة**:
   - `OKX` → للتداول الحقيقي
   - `OKX Demo Trading` → للتداول التجريبي

3. **النظام يتعامل تلقائياً**: عند اختيار "OKX Demo Trading"، النظام:
   - يضيف header: `x-simulated-trading: 1`
   - يضبط testnet flag تلقائياً
   - يستخدم نفس API keys

## خطوات الإعداد

### 1. إنشاء API Keys (مرة واحدة فقط)

1. سجل دخول إلى OKX (الحساب الحقيقي)
2. اذهب إلى Profile → API
3. أنشئ API Key مع:
   - Trade permission
   - Passphrase قوي
4. احفظ: API Key, Secret Key, Passphrase

### 2. إضافة للتداول التجريبي

في Orbitra AI:
1. Settings → API Settings
2. اختر **"OKX Demo Trading"**
3. أدخل نفس المفاتيح من الخطوة 1
4. احفظ

### 3. إضافة للتداول الحقيقي (اختياري)

إذا أردت التداول الحقيقي لاحقاً:
1. أضف مفتاح جديد
2. اختر **"OKX"** (بدون Demo)
3. استخدم نفس المفاتيح
4. احفظ

## الفرق بين OKX و OKX Demo

| الميزة | OKX | OKX Demo |
|--------|-----|----------|
| API Keys | نفس المفاتيح | نفس المفاتيح |
| الأموال | حقيقية 💰 | وهمية 🎮 |
| Header | عادي | `x-simulated-trading: 1` |
| المخاطر | حقيقية ⚠️ | بدون مخاطر ✅ |

## ملاحظات مهمة

⚠️ **تحذير**: 
- نفس API Keys تعمل للحسابين
- الفرق فقط في اختيار المنصة في النظام
- تأكد دائماً من اختيار المنصة الصحيحة!

✅ **نصيحة**:
- ابدأ دائماً بـ "OKX Demo Trading"
- اختبر استراتيجياتك
- عندما تكون واثقاً، انتقل لـ "OKX"

## استكشاف الأخطاء

### الخطأ: "API key validation failed"

**الحل**:
1. تأكد من نسخ المفاتيح بشكل صحيح
2. تأكد من Passphrase صحيح
3. تأكد من API Key له صلاحية Trade

### الخطأ: "Insufficient balance"

**الحل**:
- في Demo mode، افتح OKX وفعّل Demo Trading
- ستحصل على أموال وهمية تلقائياً

### الخطأ: "Order not executing"

**الحل**:
1. تحقق من `auto_trade_logs`
2. تأكد من اختيار "OKX Demo Trading" وليس "OKX"
3. تأكد من تفعيل Demo mode في OKX

## التحقق من النجاح

### في Orbitra AI:
```sql
SELECT platform, testnet FROM api_keys WHERE platform LIKE 'okx%';
```
يجب أن ترى:
- `okx-demo` → `testnet: true`
- `okx` → `testnet: false`

### في OKX:
1. افتح OKX
2. فعّل Demo Trading (الزر في الأعلى)
3. اذهب إلى Orders
4. يجب أن ترى الأوامر هناك

---

**الخلاصة**: نفس API Keys، منصات مختلفة في النظام! 🎯
