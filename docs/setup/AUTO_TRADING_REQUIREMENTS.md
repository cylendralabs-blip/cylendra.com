# متطلبات التداول التلقائي - Auto Trading Requirements

## ✅ الشروط المطلوبة لدخول البوت في الصفقات تلقائياً:

### 1. **تفعيل البوت الأساسي**
   - ✅ `is_active = true` (البوت مفعل)
   - ✅ `default_platform` (منصة API محددة)

### 2. **تفعيل التداول التلقائي**
   - ✅ `auto_trading_enabled = true` (التداول التلقائي مفعل)
   - ✅ `auto_trading_mode = 'full_auto'` (وضع التداول التلقائي الكامل)

### 3. **إعدادات مصادر الإشارات**
   - ✅ `allowed_signal_sources` (يجب اختيار مصدر واحد على الأقل):
     - `ai_ultra` - AI Ultra Signals
     - `ai_realtime` - AI Realtime Signals
     - `tradingview` - TradingView Signals
     - `legacy` - Legacy / Other

### 4. **إعدادات الاتجاهات**
   - ✅ `allowed_directions` (يجب اختيار اتجاه واحد على الأقل):
     - `long` - صفقات الشراء
     - `short` - صفقات البيع

### 5. **API Keys**
   - ✅ يجب أن يكون لديك API keys نشطة للمنصة المحددة
   - ✅ يجب أن تكون API keys مفعلة (`is_active = true`)

### 6. **إعدادات إضافية (اختيارية)**
   - `min_signal_confidence` - الحد الأدنى لثقة الإشارة (افتراضي: 70%)
   - `max_auto_trades_per_day` - الحد الأقصى للصفقات التلقائية يومياً
   - `max_concurrent_auto_positions` - الحد الأقصى للصفقات المتزامنة

---

## 🔍 كيف تتحقق من أن كل شيء جاهز:

### الخطوة 1: افتح Bot Settings
1. اذهب إلى `/dashboard/bot-settings`
2. افتح تبويب "التداول التلقائي" (Auto Trading)

### الخطوة 2: تحقق من الإعدادات
- ✅ **تفعيل البوت**: يجب أن يكون "تفعيل البوت" (is_active) مفعل
- ✅ **المنصة الافتراضية**: يجب أن تكون محددة
- ✅ **Auto Trading Enabled**: يجب أن يكون مفعل
- ✅ **Auto Trading Mode**: يجب أن يكون "Full Auto"
- ✅ **Allowed Signal Sources**: يجب أن يكون هناك مصدر واحد على الأقل محدد
- ✅ **Allowed Directions**: يجب أن يكون هناك اتجاه واحد على الأقل محدد

### الخطوة 3: تحقق من API Keys
1. اذهب إلى `/dashboard/api-settings`
2. تأكد من وجود API keys نشطة للمنصة المحددة

---

## ⚠️ ملاحظات مهمة:

### البوت لن يدخل في الصفقات إذا:
- ❌ `is_active = false` (البوت غير مفعل)
- ❌ `auto_trading_enabled = false` (التداول التلقائي غير مفعل)
- ❌ `auto_trading_mode = 'off'` (وضع التداول التلقائي معطل)
- ❌ `allowed_signal_sources` فارغ (لا توجد مصادر إشارات محددة)
- ❌ `allowed_directions` فارغ (لا توجد اتجاهات محددة)
- ❌ لا توجد API keys نشطة
- ❌ `default_platform` غير محدد
- ❌ الإشارة لا تطابق الفلاتر (مصدر، اتجاه، ثقة)
- ❌ تم الوصول للحد الأقصى من الصفقات اليومية
- ❌ تم الوصول للحد الأقصى من الصفقات المتزامنة

---

## 🔄 كيف يعمل التداول التلقائي:

1. **جدولة auto-trader-worker**: ⚠️ **مطلوب!** يجب جدولة `auto-trader-worker` كـ cron job
2. **استقبال الإشارة**: عندما تصل إشارة جديدة من المصدر المحدد
3. **التحقق من الفلاتر**: 
   - ✅ البوت مفعل
   - ✅ التداول التلقائي مفعل
   - ✅ المصدر مسموح
   - ✅ الاتجاه مسموح
   - ✅ الثقة كافية
   - ✅ لم يتم الوصول للحدود
4. **تنفيذ الصفقة**: إذا اجتازت جميع الفلاتر، يتم تنفيذ الصفقة تلقائياً
5. **التسجيل**: يتم تسجيل الصفقة في `auto_trades` و `auto_trade_logs`

## ⚠️ **مهم جداً: جدولة auto-trader-worker**

**بدون جدولة، البوت لن يعمل!**

راجع [AUTO_TRADING_SETUP_GUIDE.md](./AUTO_TRADING_SETUP_GUIDE.md) لمعرفة كيفية الجدولة.

---

## 📊 أين ترى الصفقات التلقائية:

1. **Auto Trade History**: `/dashboard/auto-trades/history`
2. **Trade History**: `/dashboard/trading-history` (فلتر Auto Trades)
3. **AI Live Center**: في الشارت مع overlays
4. **Auto Trading Panel**: في AI Live Center

## ⚠️ **مشكلة الصفقات اليدوية لا تظهر على Binance:**

إذا كانت الصفقات اليدوية تظهر في سجل التداول لكن لا تظهر على Binance:

**السبب:** `autoExecute = false` في صفحة Execute Trade

**الحل:**
1. افتح `/dashboard/execute-trade`
2. فعّل **"التنفيذ التلقائي"** (Auto Execute)
3. يجب أن ترى: **"✅ التنفيذ التلقائي مُفعل"**
4. نفذ الصفقة مرة أخرى

**ملاحظة:** إذا كان `autoExecute = false`، الصفقة ستكون محلية فقط (في قاعدة البيانات) ولن تُنفذ على Binance.

---

## 🛠️ استكشاف الأخطاء:

إذا كان البوت لا يدخل في الصفقات:

1. **تحقق من Console Logs**:
   - افتح Developer Tools (F12)
   - ابحث عن أخطاء في Console

2. **تحقق من Auto Trade Logs**:
   - اذهب إلى `/dashboard/auto-trades/history`
   - ابحث عن صفقات برمز `rejected`
   - انقر على الصفقة لرؤية السبب

3. **تحقق من Edge Function Logs**:
   - اذهب إلى Supabase Dashboard
   - Edge Functions → auto-trader-worker
   - تحقق من Logs

4. **تحقق من الإعدادات**:
   - تأكد من أن جميع الإعدادات المطلوبة محددة
   - تأكد من حفظ الإعدادات بعد التعديل

---

## ✅ قائمة التحقق السريعة:

- [ ] **auto-trader-worker مجدول** (cron job) ⚠️ **مطلوب!**
- [ ] البوت مفعل (`is_active = true`)
- [ ] التداول التلقائي مفعل (`auto_trading_enabled = true`)
- [ ] وضع التداول التلقائي = Full Auto (`auto_trading_mode = 'full_auto'`)
- [ ] مصدر إشارات واحد على الأقل محدد
- [ ] اتجاه واحد على الأقل محدد
- [ ] منصة API محددة (`default_platform`)
- [ ] API keys نشطة للمنصة
- [ ] API keys لديها صلاحيات Trading (ليس Read Only)
- [ ] الإعدادات محفوظة
- [ ] توجد إشارات بحالة `PENDING`

**إذا كانت جميع العناصر محددة ✅، البوت سيدخل في الصفقات تلقائياً عند وصول إشارات مطابقة للفلاتر!**

**⚠️ ملاحظة مهمة:** بدون جدولة `auto-trader-worker`، البوت لن يعمل حتى لو كانت جميع الإعدادات صحيحة!

راجع [AUTO_TRADING_SETUP_GUIDE.md](./AUTO_TRADING_SETUP_GUIDE.md) لمعرفة كيفية الجدولة.

