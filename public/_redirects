# Netlify redirects file for SPA routing
# This file ensures all routes redirect to index.html for client-side routing
# It's a backup in case netlify.toml redirects don't work

/*    /index.html   200

